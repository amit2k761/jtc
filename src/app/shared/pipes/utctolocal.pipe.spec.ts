import { UtctolocalPipe } from './utctolocal.pipe';

describe('UtctolocalPipe', () => {
  it('create an instance', () => {
    const pipe = new UtctolocalPipe();
    expect(pipe).toBeTruthy();
  });
});
